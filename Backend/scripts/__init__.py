"""Backend scripts — deliberately-run tooling (seed support, fixture export).

Nothing here is imported by the running app; these are invoked by name
(AGENTS.md §2.3 — seed/test data lives in clearly-named scripts only).
"""

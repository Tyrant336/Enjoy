# Session 004 — 2026-09-19 — Build plan (docs/BUILDING.md)

## Goal
Create the build/orchestration plan for 1 monitor + 3 builder agents, grounded in
the actual codebase state.

## Scanned state
- `frontend/` = stock create-next-app (Next 16, React 19, Tailwind 4); no R3F/
  three/zustand yet; the 3 locked GLBs are in `public/models/`.
- `Backend/` = empty. No `docker-compose.yml`. `.env.example` exists
  (OpenRouter, `DATABASE_URL` postgres://localhost:5432/harbour, port 8000).
- Docs: REQUIREMENTS v2.1 (contracts frozen), LABELS, RESEARCH, plan, sessions 001–003.
- opensource/01–07 cloned with distill specs.

## What was done
- Wrote `docs/BUILDING.md` v1.0:
  - Team topology: Monitor (contracts/merges/E2E/cut-line) + Agent A (Backend) +
    Agent B (FE above-water world) + Agent C (FE interaction: atlas, worldBus/SSE,
    chat UI, tour, a11y).
  - Exclusive ownership zones; shared files serialized by monitor.
  - 5 phases (0 scaffold+contract freeze → 1 seeded verticals → 2 core loops →
    3 integration+demo hardening → 4 P1 stretch) with exit tests + checkpoints.
  - 72-hour timeline mapping, daily rhythm, explicit cut-line.
  - Working agreements (frozen contracts, B↔C `worldApi.ts` at Checkpoint 1,
    branch-per-agent) + risk register (incl. coverage-vs-speed decision rule).
- Updated `README.md` docs table with BUILDING.md.

## State at end of session
Ready to start Phase 0. First actions: monitor creates `docker-compose.yml` +
type files; A scaffolds FastAPI+Alembic; B installs R3F stack; C ports atlas.js.

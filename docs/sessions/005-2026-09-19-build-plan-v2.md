# Session 005 — 2026-09-19 — Build plan v2 (team restructure)

## Goal
Rebuild `docs/BUILDING.md` per owner decision: scaffolding is a soft task for ONE
agent; the 3D/three.js work is the big task for ONE dedicated agent.

## What changed (BUILDING.md 1.0 → 2.0)
- Team: Monitor + **Agent S** (Scaffold → then Support) + **Agent T** (ALL three.js:
  above-water world, underwater atlas layer, camera, review POV) + **Agent L**
  (backend logic + FE non-3D: chat/task-sheet/tour/banners/SSE bus).
- Phase 0 is Agent S solo (docker, Backend skeleton, deps, contract type files);
  T preps (studies atlas.js, prototypes water in a `lab` route), L preps
  (design notes + writes the §4.6 seed fixture JSON).
- After Phase 0, S converts to Support (seeds, tests, devops, unblocker).
- T↔L interface = `worldApi.ts` (S drafts P1, T implements, L consumes; frozen at
  Checkpoint 1). Critical path = Agent T; plan shields T from non-3D chores.
- Timeline re-mapped: H0–4 scaffold / H4–28 verticals / H28–52 loops /
  H52–66 integration / H66–72 buffer.

## State at end of session
Ready for Phase 0: Agent S starts with `docker-compose.yml` + Backend skeleton +
deps + `schemas.py`/`types.ts`.

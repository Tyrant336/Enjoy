# Session 026 — P1 Plan: builder prompt + acceptance checklist (monitor)

**Date:** 2026-09-19 · **Hat:** Monitor (plan + verify only, no feature code)

## Goal

Owner directive: *"Finish P1. Real done. I don't want to see P1 again."*
A separate agent owns all FE work in parallel; P1 here = **backend only**.
The monitor's job this session: produce the builder's prompt and the binary
checklist that decides "P1 is finished".

## What was done

1. **Builder prompt:** `docs/handoff/agent-l-p1.md` — complete P1 backend
   handoff: read-first list, ownership boundaries, verified current state,
   8 locked design decisions, the two §5.2 endpoints, 7-stage work order,
   fixture/mocking rules, done criteria, FE coordination list.
2. **Acceptance checklist:** `docs/P1-ACCEPTANCE-CHECKLIST.md` — 24 binary
   monitor-executed checks (gates/hygiene, ingestion, flashcard-gen, KG-gen,
   FSRS, scheduler/orchestrator LLM upgrade, golden-path E2E, FE handoff items).
3. Fixed a session-numbering collision from earlier today: the four-agent eval
   session file is now `docs/sessions/025-2026-09-19-four-agent-eval.md`
   (024 was taken by the pitch-slide session).

## Key locked decisions (so the builder cannot drift)

- **Docling → text → text-LLM.** No page images; the configured text-only
  OpenRouter model is fine; the `.env.example` vision warning does not apply.
- **One LLM wrapper** (`app/core/llm.py`, langchain-openai + structured output);
  **one shared ingestion pipeline** (FR-2 + FR-3).
- LangGraph only where it earns its keep: per-artifact generation graphs,
  scheduler plan-execute-replan (StudyPlan contract unchanged), orchestrator
  LLM-supervisor fallback node (deterministic pre-router stays first).
- py-fsrs replaces P0 fixed intervals (grade API + FR-2.9 unchanged); genanki
  `.apkg` at generation time; KeyBERT is the ONLY sanctioned fallback
  (WARNING + `fallbackUsed` in response).
- Template planner is DELETED when the LangGraph planner lands (§3.6).
- Builder must also fix eval defects D1–D5 (session 025).
- Test fixtures: the 3 PDFs from session 025 → `Backend/tests/fixtures/`.

## Verified by execution this session

- Backend suite re-run: **132 passed / 2 skipped, coverage 97.67%** (gate 90%).
- Session-file audit (001–025) performed earlier today informed the checklist;
  its permanent open items (junk-plan bug = D3/D4, template tasks = D5) are
  folded into the builder scope.

## NOT verified / open

- The builder has not started; nothing in this session changed product code.
- The FE build is still broken (in-flight `SmallBoat.tsx` signature change) —
  owned by the FE agent (checklist H4).
- `deepseek/deepseek-v4.1-flash` model validity on OpenRouter must be proven by
  the builder in Stage 1 (a real hello call); if invalid → owner decision.

## State at end

- P1 plan is fully specified; execution is next. Monitor verifies with
  `docs/P1-ACCEPTANCE-CHECKLIST.md` — 100% PASS = P1 done.
